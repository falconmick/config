return {
  "stevearc/conform.nvim",
  opts = {
    -- format_on_save = {
    --   -- Timeout (ms)
    --   timeout_ms = 500,
    --   -- Which filetypes should auto-format with Prettier
    --   lsp_fallback = true,
    -- },
  },
}
